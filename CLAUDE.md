@AGENTS.md

# MapCanva

Read these before doing anything in this repo — they are the source of truth:

- `.claude/rules/00-workflow.md` — how we work. **Triage every request to
  reject / backlog / a named sprint before writing any code.** Never mark a
  ticket DONE; only the product owner does that.
- `.claude/rules/01-product.md` — what we are building and who for.
- `.claude/rules/02-architecture.md` — folder map and state model.
- `.claude/rules/03-gotchas.md` — mistakes already paid for. Read before
  debugging anything odd; add to it when something bites.

Current work: `.claude/boards/` (sprints and backlog), `.claude/tickets/`.

We are on **v0**, working toward **MVP v1**.
